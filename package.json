{
  "name": "ttv-emote-stats",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "dev": "lerna run dev --stream --no-bail",
    "build": "lerna run build --stream --no-bail",
    "start": "lerna run start --stream --no-bail",
    "compile-readme": "cd docs && node buildReadme.js"
  },
  "resolutions": {
    "glob-parent": "^6.0.1",
    "electron": "^11.5.0",
    "tar": "^6.1.9",
    "set-value": "^4.0.1",
    "nth-check": "^2.0.1",
    "ansi-regex": "^5.0.1"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "markdown-include": "^0.4.3"
  }
}
